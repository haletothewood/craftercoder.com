apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "craftercoder-chart.fullname" . }}-test-connection"
  labels:
{{ include "craftercoder-chart.labels" . | indent 4 }}
  annotations:
    "helm.sh/hook": test-success
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args:  ['{{ include "craftercoder-chart.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
